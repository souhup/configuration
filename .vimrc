syntax on
set nocompatible "关闭vi兼容模式
set number "显示行号
set cursorline "突出当前行
set ruler "打开状态栏标尺

set tabstop=2 "tab长度为2空格
set shiftwidth=2 "使用<和>移动时宽度为2空格
set expandtab "将已有的tab转为空格

set nowrapscan "禁止在搜索到文件两端时重新搜索
set incsearch "搜索内容时就显示搜索结果
set hlsearch "搜索时高亮提示被找到的文本
set smartindent "开启新行时自动缩进
set backspace=indent,eol,start "使用backspace和delete可以删除回车和之前的字符

set encoding=utf-8 "文本编码utf-8


imap jk <Esc>

